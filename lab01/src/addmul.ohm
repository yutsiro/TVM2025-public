AddMul {
    Expr = AddExpr  -- main
    AddExpr = AddExpr "+" MulExpr  -- add
            | MulExpr  -- mul
    MulExpr = MulExpr "*" Atom  -- mul
            | Atom  -- pri
    Atom = "(" Expr ")"  -- paren
            | number  -- num
    number = digit+  -- digits
}
